// 引入解析token方法
import jwt_decode from 'jwt-decode'

export function token (account:string, password:string) {
  let token:string
  try{
    const date = 1000
  } catch (err) { 
    console.log(err)
    return null
  }
  return token
}